<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Yukki.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Yukki"
    Title=""
    FlyoutBehavior="Flyout"
    >

    <FlyoutItem Title="Início" Icon="menu.png">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <!-- Rota para sacola (exemplo simples) -->
    <ShellContent Title="CartPage" Route="Cart" ContentTemplate="{DataTemplate local:CartPage}" />

</Shell>
